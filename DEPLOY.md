# Інструкції для деплою на сервер

## 🔧 C++ модуль - що потрібно зробити

### ⚠️ Важливо:
C++ модуль **НЕ запускається автоматично** - він викликається через `subprocess.run()` коли потрібно. Але **потрібно скомпілювати** його на сервері!

### 📋 Кроки для деплою:

1. **Завантажте проект на сервер** (через git, FTP, тощо)

2. **Встановіть залежності Python:**
```bash
pip install -r requirements.txt
```

3. **Скомпілюйте C++ модуль** (обов'язково!):
```bash
cd cpp_analytics
make
cd ..
```

4. **Перевірте, чи файл створено:**
```bash
ls -la cpp_analytics/analytics
# Має бути виконуваний файл
```

5. **Надайте права на виконання** (якщо потрібно):
```bash
chmod +x cpp_analytics/analytics
```

6. **Виконайте міграції Django:**
```bash
python manage.py migrate
```

7. **Запустіть Django сервер:**
```bash
python manage.py runserver
# або через gunicorn/uvicorn для production
```

### 🔍 Як працює C++ модуль:

- C++ модуль **НЕ працює як окремий сервіс**
- Він викликається **на вимогу** через `subprocess.run()` коли:
  - Користувач відкриває сторінку звітів
  - Генерується PDF звіт
  - Викликається API `/api/analytics/`

- Python передає JSON через `stdin`
- C++ обчислює аналітику
- C++ повертає JSON через `stdout`
- Python обробляє результати

### ✅ Перевірка роботи:

Після деплою перевірте:
1. Відкрийте сторінку звітів - має працювати
2. Згенеруйте PDF звіт - має містити дані з C++
3. Перевірте логи Django - не має бути помилок про відсутність C++ модуля

### 🐛 Якщо C++ модуль не працює:

Система має резервний варіант через Python ORM, тому:
- Веб-інтерфейс працюватиме
- Звіти працюватимуть (але без ABC-аналізу та деяких статистик)
- PDF звіти працюватимуть (з резервними даними)

### 📝 Автоматизація (опційно):

Можна створити скрипт `deploy.sh`:
```bash
#!/bin/bash
cd cpp_analytics
make
cd ..
python manage.py migrate
# інші команди деплою
```

